import type { Config } from 'tailwindcss'

export default <Partial<Config>>{
  theme: {
    fontFamily: {
      sans: ["'Lexend'", "'-apple-system'", "'system-ui'", "'BlinkMacSystemFont'", "'Arial'", "'sans-serif'"],
    }
  }
}